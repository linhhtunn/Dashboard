[project]
name = "health-rabbit-mq"
version = "0.1.0"
description = "RabbitMQ/CloudAMQP utilities for the health simulation pipeline"
requires-python = ">=3.10"
dependencies = [
  "pika>=1.3.2,<2.0.0",
]

[tool.uv]
package = false

